{
  "private": true,
  "scripts": {
    "build": "eslint src && pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "eslint": "^4.8.2",
    "purescript": "^0.14.3",
    "pulp": "^15.0.0",
    "purescript-psa": "^0.7.0"
  },
  "dependencies": {
    "@math.gl/web-mercator": "^3.2.2"
  }
}
