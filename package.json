{
  "private": true,
  "scripts": {
    "build": "eslint src && pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "eslint": "^3.17.1",
    "purescript": "^0.12.0",
    "pulp": "^12.2.0",
    "purescript-psa": "^0.7.0"
  },
  "dependencies": {
    "viewport-mercator-project": "^5.1.0"
  }
}
